{
  "name": "exit-intent-modal",
  "version": "1.0.0",
  "description": "modal offer plugin",
  "main": "",
  "scripts": {
    "build-css": "node-sass --include-path scss styles/main.scss styles/main.css --output-style",
    "watch-css": "nodemon -e scss -x \"npm run build-css\" --output-style compressed",
    "build-css-compressed": "node-sass --include-path scss styles/main.min.scss styles/main.min.css --output-style compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vherever",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.3",
    "nodemon": "^2.0.20"
  }
}
